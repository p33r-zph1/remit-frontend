/** @type {import("prettier").Options} */
const config = {
  arrowParens: 'avoid',
  semi: true,
  tabWidth: 2,
  singleQuote: true,
  trailingComma: 'es5',
  endOfLine: 'auto',
};

module.exports = config;
